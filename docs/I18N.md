# Preferred language (i18n foundation)

## Shipped

- `User.preferredLocale` (BCP-47, default `en-GB`) — Prisma + Netlify SQL migration
- Settings → Appearance control (`SUPPORTED_LOCALES` in `lib/i18n.ts`)
- `PATCH /api/auth/me` `{ preferredLocale }` + `GET` returns it
- `LocaleProvider` / `useLocale()` — localStorage cache + account sync
- High-visibility strings: header “Commentary desk”, feed sync / linked labels

## Next steps (not in this slice)

1. Expand `STRINGS` catalogue (desk chrome, Settings tabs, auth copy)
2. Set `<html lang>` from preference (layout is server — cookie or middleware)
3. Date/number formatting via `Intl` with preferred locale
4. Optional per-desk commentary language separate from UI locale
